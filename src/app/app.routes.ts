import { Routes } from '@angular/router';
import { List } from './components/list/list';
import { App } from './app';

export const routes: Routes = [
    {
        path: '',
        component: List
    }
];
